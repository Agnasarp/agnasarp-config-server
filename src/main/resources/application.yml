server:
  port: 8580

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Agnasarp/agnasarp-microservice-configurations
          clone-on-start: true
  profiles:
    active: native